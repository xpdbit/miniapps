/** Drawer 宽度（响应式） */
export const DRAWER_WIDTH = { mobile: '100%' as const, desktop: 600 as const }

/** Modal 宽度（响应式） */
export const MODAL_WIDTH = { mobile: '100%' as const, desktop: 520 as const }

/** 宽版 Drawer 宽度（如主题编辑器） */
export const DRAWER_WIDTH_WIDE = { mobile: '100%' as const, desktop: 680 as const }
